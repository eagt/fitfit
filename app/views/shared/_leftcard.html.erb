<div class="col-4">

<div class="card card-outline-info card-responsive"> <!-- This will outline all card. But I can outline each section if wanted -->
  	<div class="card-header text-info text-center text-responsive">
       <h7><%= @user.name + " " + @user.surname %></h7>
 	 </div>
    <div class="card-block card-inverse card-info">
      <%= icon('id-card-o', class: 'fa-5x') %> 
    </div>

    <div class="card-footer">
    	<td> <%= icon('info') %></td>
	    <td><%= link_to 'Details', @user  %></td>
    </div> 
  </div>
</div>