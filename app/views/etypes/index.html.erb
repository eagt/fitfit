<div class="row">
        <%= render partial: "shared/leftcard" %>
        <%= render partial: "shared/centrecard" %>
        <%= render partial: "shared/rightcard" %>     
</div>


<div class="row">&nbsp;</div>

<div class="card card-outline-success">
  <div class="card-header card-inverse card-success text-center">
    <p id="notice"><%= notice %></p>
    <h1>Etypes</h1>
  </div>

 <div class="card-block">
    <table class="table table-responsive table-striped">
      <thead class="thead-inverse">

    <tr>
      <th>Name</th>
      <th>Actions</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody class="inshtaex">
    <% @user.etypes.each do |etype| %>
      <tr>
        <td><%= etype.name %></td>
        <td><%= link_to 'Show', user_etype_path(@user, etype) %></td>
        <td><%= link_to 'Edit', edit_user_etype_path(@user, etype) %></td>
        <td><%= link_to 'Destroy', [@user, etype], confirm: 'Are you sure?', method: :delete %></td>

      </tr>
    <% end %>
  </tbody>
</table>

<br>

<div class="card-footer card-inverse card-success">
    <%= link_to(new_user_etype_path, :class=>"card-inverse") do %>
    <%= icon('file-text-o','New Etype', class: 'fa-2x') %> 
    <% end %> 
</div>  
